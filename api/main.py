"""
FastAPI backend — stub for Day 3 implementation.

Will expose:
- POST /research — create a research job
- GET /research/{job_id} — get job status/result
- GET /health — health check
"""

# TODO (Day 3): Implement FastAPI application with:
# - Job-based async research execution
# - Persistent job storage (SQLite or LangGraph checkpointing)
# - Structured JSON responses
# - Health endpoint
